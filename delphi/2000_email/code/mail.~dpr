program mail;

uses
  Forms,
  main_Unit in 'main_Unit.pas' {Main},
  User_Unit in 'User_Unit.pas' {User},
  get_Unit in 'get_Unit.pas' {Get},
  send_Unit in 'send_Unit.pas' {Send},
  adressbuch_Unit in 'adressbuch_Unit.pas' {Adressbuch},
  Neueintrag_Unit in 'Neueintrag_Unit.pas' {Neueintrag};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TMain, Main);
  Application.CreateForm(TUser, User);
  Application.CreateForm(TGet, Get);
  Application.CreateForm(TSend, Send);
  Application.CreateForm(TAdressbuch, Adressbuch);
  Application.CreateForm(TNeueintrag, Neueintrag);
  Application.Run;
end.
